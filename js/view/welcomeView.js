
var WelcomeView = function (container, model) {
	// Hiding the select-dish-view
	container.find("#select-dish-view").hide();

	// hidinng the dinner overview view
	container.find("#dinner-overview-view").hide();

	// hiding the dinner printout view
	container.find("#dinner-printout-view").hide();
}